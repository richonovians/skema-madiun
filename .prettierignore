# Dependency & build output
node_modules
dist
.next
build
coverage

# Lockfile & generated
pnpm-lock.yaml
**/prisma/generated

# Dokumen sumber (jangan diformat ulang agar tabel/isi tetap utuh)
docs

# Frontend Next.js: gaya kode dikelola oleh setup-nya sendiri (eslint-config-next),
# bukan Prettier root — hindari reformat masal kode tim frontend.
apps/web
